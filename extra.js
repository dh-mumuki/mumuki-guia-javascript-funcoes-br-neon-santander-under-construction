function longitude(sequencia) {
  return sequencia.length; 
}

function alcance(s, e) {
  let r = [];
  for (let i = s; i <= e; i++) {
     r.push(i)
  }
  return r;
}

function imprimir(s) {
  console.log(s)
}

function colocar(a, e) {
   array.push(e);
}

function absoluto(x) {
  return Math.abs(x)
}

function converterEmMaiuscula(x) {
  return x.toUpperCase()
}

function comecaCom(x, y) {
  return x.startsWith(x, y);
}
